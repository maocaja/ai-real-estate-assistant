from setuptools import setup, find_packages

setup(
    name="ai_real_estate_assistant",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[],  # Las dependencias ya están en requirements.txt
) 